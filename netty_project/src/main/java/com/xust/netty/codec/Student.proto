syntax = "proto3"; //版本
option java_outer_classname = "StudentPOJO"; //生成的外部类名，同时也是文件名
//protobuf是以message管理数据
message Student {
    int32 id = 1;//有一个属性名为id，类型为int32(protobuf类型),1代表序号
    string name = 2;
}